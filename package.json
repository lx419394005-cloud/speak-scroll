{
  "name": "english-speaker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,worker,client -c cyan,yellow,magenta \"npm run dev --prefix server\" \"npm run dev --prefix worker\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:worker": "npm run dev --prefix worker",
    "dev:client": "npm run dev --prefix client",
    "build": "npm ci --prefix client && npm run build --prefix client",
    "deploy": "npm run build && npm run deploy --prefix worker",
    "deploy:cf": "bash ./scripts/cf-deploy.sh",
    "deploy:leaderboard": "npm run deploy --prefix worker",
    "test": "npm run test --prefix client",
    "test:unit": "npm run test:unit --prefix client",
    "test:e2e": "npm run test:e2e --prefix client"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
